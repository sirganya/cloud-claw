/** Appends normalized, unique environment-variable candidates to a keyed bucket. */
export declare function appendUniqueEnvVarCandidates(target: Record<string, string[]>, ownerId: string, keys: readonly string[]): void;
