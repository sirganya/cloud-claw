import type { GatewayRequestHandlers } from "./types.js";
/** Gateway handlers for identity, heartbeat toggles, and system presence events. */
export declare const systemHandlers: GatewayRequestHandlers;
