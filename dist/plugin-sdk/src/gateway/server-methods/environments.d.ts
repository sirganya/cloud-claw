import type { GatewayRequestHandlers } from "./types.js";
/** Gateway handlers for querying local and node execution environments. */
export declare const environmentsHandlers: GatewayRequestHandlers;
