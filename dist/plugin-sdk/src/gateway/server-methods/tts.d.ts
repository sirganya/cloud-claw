import type { GatewayRequestHandlers } from "./types.js";
/** Gateway request handlers for TTS status, preference mutation, and synthesis. */
export declare const ttsHandlers: GatewayRequestHandlers;
