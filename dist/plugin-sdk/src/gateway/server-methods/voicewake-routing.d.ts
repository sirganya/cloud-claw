import type { GatewayRequestHandlers } from "./types.js";
/** Gateway request handlers for reading and updating voice wake routing. */
export declare const voicewakeRoutingHandlers: GatewayRequestHandlers;
