import type { GatewayRequestHandlers } from "./types.js";
/** Handles `tools.invoke` with protocol-shaped success and failure payloads. */
export declare const toolsInvokeHandlers: GatewayRequestHandlers;
