import type { GatewayRequestHandlers } from "./types.js";
/** Gateway request handlers for reading and updating voice wake triggers. */
export declare const voicewakeHandlers: GatewayRequestHandlers;
