import type { GatewayRequestHandlers } from "./types.js";
/** Gateway request handlers for invoking registered native hook relays. */
export declare const nativeHookRelayHandlers: GatewayRequestHandlers;
