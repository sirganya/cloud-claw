import type { GatewayRequestHandlers } from "./types.js";
/** Gateway handlers for plugin-declared Control UI descriptors and session actions. */
export declare const pluginHostHookHandlers: GatewayRequestHandlers;
