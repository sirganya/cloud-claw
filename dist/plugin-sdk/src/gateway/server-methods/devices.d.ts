import type { GatewayRequestHandlers } from "./types.js";
/** Gateway request handlers for device pair approval, removal, token rotation, and revocation. */
export declare const deviceHandlers: GatewayRequestHandlers;
