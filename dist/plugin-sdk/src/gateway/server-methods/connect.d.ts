import type { GatewayRequestHandlers } from "./types.js";
/**
 * Rejects `connect` after the WebSocket handshake already established identity.
 */
export declare const connectHandlers: GatewayRequestHandlers;
