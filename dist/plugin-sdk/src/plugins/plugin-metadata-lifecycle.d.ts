/** Registers a process-local plugin metadata memo clear hook. */
export declare function registerPluginMetadataProcessMemoLifecycleClear(clearProcessMemo: () => void): void;
/** Clears plugin metadata snapshots and registered process memo caches. */
export declare function clearPluginMetadataLifecycleCaches(): void;
