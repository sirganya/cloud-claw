import type { ChannelRuntimeContextRegistry } from "../../channels/plugins/channel-runtime-surface.types.js";
/** Creates the in-memory channel runtime context registry used by plugin runtime surfaces. */
export declare function createChannelRuntimeContextRegistry(): ChannelRuntimeContextRegistry;
