import type { PluginMetadataSnapshot } from "../plugins/plugin-metadata-snapshot.types.js";
export declare const PLUGIN_MODEL_CATALOG_FILE = "catalog.json";
export declare const PLUGIN_MODEL_CATALOG_GENERATED_BY = "openclaw-plugin-model-catalog-v1";
export type PluginModelCatalogMetadataSnapshot = Pick<PluginMetadataSnapshot, "owners"> & {
    index?: {
        plugins: ReadonlyArray<{
            enabled: boolean;
            pluginId: string;
        }>;
    };
    normalizePluginId?: (pluginId: string) => string;
};
type PluginModelCatalogFile = {
    path: string;
    pluginId: string;
    relativePath: string;
};
/** Encodes the profile-relative path for a plugin-owned generated model catalog. */
export declare function encodePluginModelCatalogRelativePath(pluginId: string): string;
/** Returns true only for canonical profile-relative generated catalog paths. */
export declare function isPluginModelCatalogRelativePath(relativePath: string): boolean;
/** Decodes the plugin id from a canonical generated catalog path. */
export declare function decodePluginModelCatalogRelativePathPluginId(relativePath: string): string | undefined;
/** Lists deterministic generated catalog paths present in an agent profile. */
export declare function listPluginModelCatalogRelativePaths(agentDir: string): string[];
/** Lists existing generated catalog files with decoded plugin ownership. */
export declare function listPluginModelCatalogFiles(agentDir: string): PluginModelCatalogFile[];
/** Detects model catalogs generated by OpenClaw rather than user-authored JSON. */
export declare function isGeneratedPluginModelCatalog(value: unknown): boolean;
/** Resolves the sole enabled plugin that owns a provider's model catalog. */
export declare function resolvePluginModelCatalogOwnerPluginId(params: {
    providerId: string;
    pluginMetadataSnapshot?: PluginModelCatalogMetadataSnapshot;
}): string | undefined;
/** Keeps generated catalog providers only when the catalog plugin still owns them. */
export declare function filterGeneratedPluginModelCatalogProviders<T>(params: {
    catalogPluginId?: string;
    parsedCatalog?: unknown;
    pluginMetadataSnapshot?: PluginModelCatalogMetadataSnapshot;
    providers: Record<string, T>;
}): Record<string, T>;
export {};
