/** Returns true when any local/runtime/main auth profile source exists. */
export declare function hasAnyAuthProfileStoreSource(agentDir?: string): boolean;
/** Returns true when the requested agent dir has a local auth profile source. */
export declare function hasLocalAuthProfileStoreSource(agentDir?: string): boolean;
