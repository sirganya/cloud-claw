/** Resolve the directory that contains the shared state SQLite file. */
export declare function resolveOpenClawStateSqliteDir(env?: NodeJS.ProcessEnv): string;
/** Resolve the shared state SQLite file path. */
export declare function resolveOpenClawStateSqlitePath(env?: NodeJS.ProcessEnv): string;
