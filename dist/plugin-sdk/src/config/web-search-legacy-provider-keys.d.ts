/** Legacy config keys that used to live under web search provider config. */
export declare const LEGACY_WEB_SEARCH_PROVIDER_CONFIG_KEYS: Set<string>;
export declare function isLegacyWebSearchProviderConfigKey(key: string): boolean;
