import { type LegacyConfigMigrationSpec } from "../../../config/legacy.shared.js";
/** Legacy config migration specs for channel-owned compatibility keys. */
export declare const LEGACY_CONFIG_MIGRATIONS_CHANNELS: LegacyConfigMigrationSpec[];
