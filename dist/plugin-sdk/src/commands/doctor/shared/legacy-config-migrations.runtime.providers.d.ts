import { type LegacyConfigMigrationSpec } from "../../../config/legacy.shared.js";
/** Legacy config migration specs for provider/plugin runtime config compatibility. */
export declare const LEGACY_CONFIG_MIGRATIONS_RUNTIME_PROVIDERS: LegacyConfigMigrationSpec[];
