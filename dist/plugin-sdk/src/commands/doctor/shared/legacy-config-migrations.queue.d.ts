import { type LegacyConfigMigrationSpec } from "../../../config/legacy.shared.js";
/** Legacy config migration specs for message queue mode compatibility. */
export declare const LEGACY_CONFIG_MIGRATIONS_QUEUE: LegacyConfigMigrationSpec[];
