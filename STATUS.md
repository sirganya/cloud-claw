# cloud-claw — Status

_Updated 2026-08-19 by Claude_

**Last done:**

- **Telegram switched to webhook mode so it can wake a sleeping container** (polling never could). Added a `/telegram` Worker route (validates the `X-Telegram-Bot-Api-Secret-Token`, forwards to the plugin's listener on container port 8787 via `switchPort`), set `channels.telegram.webhookUrl/webhookSecret/webhookPort:8787/webhookPath/webhookHost:0.0.0.0` in the Dockerfile, and a `TELEGRAM_WEBHOOK_SECRET` Worker secret (auto-forwarded to the container). Verified live: webhook registered (`getWebhookInfo` clean, 0 pending), listener reachable on `0.0.0.0:8787`, mode=webhook. Telegram's default `streaming.mode:"progress"` gives the single-editable-message progress UX GChat lacks. Added **Liz (`8958143785`) to `allowFrom`+`groupAllowFrom`** so she can use it. Also scoped `pnpm lint` to `src/` (was breaking on / churning the vendored beta tree).
- **Known-unresolved:** the Google Chat double-typing-placeholder is a symptom of the beta breaking the Worker↔gateway keep-alive — `container.ts` offered gateway protocol v3 but the beta requires v4 (`MIN_CLIENT_PROTOCOL_VERSION=4`). Bumped it to `minProtocol:3,maxProtocol:4`, but the operator WS still shows `closed before connect` after deploy (DO may not have taken new code, or a deeper device-auth change). Telegram-webhook sidesteps this (wakes per-message). Phase 2 (Worker instant-ack for cold-start) still open.
- **Upgraded OpenClaw 2026.5.28 → 2026.8.1-beta.2** (live, verified). Chose the beta because `gemini-3.7-flash` is native there (2026.7.1 stable tops out at 3.5). Rebuilt the vendored `openclaw-build/` tree via the CI workflow (`build-dist.yml`, now dispatch-only + pinnable ref + uploads the full tree), filled `node_modules/openclaw` from the published package, and **de-risked by booting the beta against a 245MB copy of live `/data` before deploying** — caught a hard blocker (legacy `device_bootstrap_tokens` table missing `setup_id` makes the gateway refuse to start) and proved the fix. Dockerfile changes: dropped the now-obsolete `session-accessor` + `3.7-flash` patches (both fixed upstream), collapsed the fragile per-scoped-package `node_modules` COPYs into one (beta dropped `@zed-industries`), and added an idempotent entrypoint step that drops the stale table + runs `doctor --fix` before gateway start. Deploy needed a retry (transient `Unauthorized` after the long push). Live container now runs beta: gateway `/health` 200, chat bridge up, migration clean. Pre-beta rollback snapshot at `scratchpad/PREBETA-data-snapshot.tar.gz`; old tree at `openclaw-build.june-bak`.
- Switched the agent's primary model to `google/gemini-3.7-flash` (was `3.5-flash`). The pinned OpenClaw build (2026.5.28, built 2026-06-27) predates 3.7, so its Gemini forward-compat resolver returns `undefined` for the unknown id → "unknown model". Added a Dockerfile dist-patch `sed` that extends the flash-family branch in `provider-models-*.js` so 3.7-flash clones the same Flash capability template 3.5-flash uses, while the real id is sent to Google. **Greg deployed; live-verified working** — SSH'd the running container (instance created 17:14 today), confirmed `/data/openclaw.json` model = `gemini-3.7-flash` and the agent is executing multi-step tool turns on it (lead-scout run, memory writes). Remove the patch once openclaw-build is regenerated on a newer OpenClaw.
- Diagnosed a chat "Write: to /tmp/mem_append.txt failed" error: benign, self-corrected. 3.7-flash likes to stage memory edits in a `/tmp` scratch file first; OpenClaw's `write` tool only allows paths inside the workspace, so the `/tmp` write is rejected — the agent then wrote directly to `memory/2026-08-18.md` (succeeded, no data lost). OS `/tmp` is writable; it's the tool's workspace guard. Added a `<!-- cloud-claw:file-writes -->` operator rule to the **live** `/data/workspace/AGENTS.md` (backup `AGENTS.md.bak-filewrite`) telling the agent to write memory files directly, never stage in `/tmp`. R2-backed, no redeploy. Container URL: `https://cloud-claw.quickpointme.workers.dev`.
- Resolved a live "Quickly not responding" incident. The gateway had **wedged** — it acked Google Chat webhooks with HTTP 200 but dispatched nothing to the agent (no sessions/DB writes for ~2h). A container restart cleared it. Real root cause: two **poisoned ~5MB DM sessions** (Chat DM + a Telegram DM) that failed compaction ("auto-compaction could not recover this turn") and blocked the single `main` agent. Reset them via the gateway-routed `openclaw sessions cleanup --enforce --fix-missing` (archive the bloated trajectory → entry becomes "missing" → CLI drops it, memory-consistent, no R2-restore clobber). Chat DM now on a fresh session and replying.
- Audited memory (per Greg). Always-injected curated core is **~6.5k tokens/turn** and mostly well-curated — **not** the main bloat. Trimmed MEMORY.md (−249 tok/turn: dropped stale wake-claim, tightened verbose infra prose, kept all rules), deduped daily notes (2026-08-14.md was 62% duplicate; −14KB total), archived 15 daily notes >30 days old. The 150MB agent SQLite is **real data** (embeddings stored as TEXT, 86MB cache) — VACUUM freed only 466KB; it's a latency/backup cost, not a per-turn token cost.
- Confirmed the actual per-turn "pointless text to the API" is transcript/exec-inlining, not memory — and the `cloud-claw:exec-hygiene` rule is **already live** in the running AGENTS.md (R2-backed, survives restarts), so the cost fix is active now.

**Next planned:**

- **Deploy** the committed Dockerfile changes (exec-hygiene bake-in + wake-message removal) when the system is idle — deferred today because Liz was actively using it.
- Measure whether exec-hygiene actually shrinks per-turn transcript size on Liz's heavy sessions; if not, build a `beforeToolCall` guard plugin (hard enforcement — OpenClaw exposes the hook).

**Needs from Greg:** Cloudflare API token (or run `pnpm run deploy` yourself) — deploy needs it and I can't auth non-interactively; not urgent since exec-hygiene is already live. **Rotate the Notion token `ntn_6801…16Pc`** (still in old transcripts/R2). Decide commit-vs-gitignore for untracked CLAUDE.md / scripts/ / openclaw-build/ / src/admin-html.ts.

**What it is:** A self-hosted OpenClaw AI assistant ("Quickly") on Cloudflare Workers + Containers — a Worker handles auth/routing to a singleton container gateway and proxies CDP sessions via Browser Rendering.

**Current focus:** Gemini cost/performance. Established the failure mode is bloated sessions (exec-inlining) that (a) balloon per-turn context and (b) poison compaction, wedging the single agent. Addressed via the live exec-hygiene rule + memory cleanup; deploy + heavy-session measurement still pending.

**In flight (uncommitted):** Wake-message removal committed (d777f46) + pushed but **NOT deployed** (system in use). Live container has server-side changes not in git: reset DM sessions, trimmed MEMORY.md, deduped + pruned daily notes (backups at `MEMORY.md.bak-trim`, `memory.bak-dedup`, `memory-archive/`). Untracked repo files still undecided.
